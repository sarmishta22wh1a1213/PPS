#include <stdio.h>

int main(){
	float r , pi, area ;
	pi = 3.14 ;
	printf("enter the value of radius of the circle - ");
	scanf("%f", &r);
        area =( pi * r * r) ;
	printf("the area of the circle is %f", area );
      return 0;
}

