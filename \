#include <stdio.h>
#include <math.h>

int main(){
	float a ,t,u,s;
	printf("enter the value of initial speed of the bus - ");
	scanf("%f",&u);
	printf("enter the value of time taken for the bus to reach the final destination");
	scanf("%f", &t);
	printf("enter the acceleration of the bus");
	scanf("%f", &a);
	s = u * t +( 1/2) * a * t * t ;
	printf("the distance covered by the bus is = %f",s);
	return 0 ;
}
