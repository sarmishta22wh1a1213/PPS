#include <stdio.h>
int main (){
	int age;
	char star;
	float decimal;
	long long int num;
	long int num1;
	printf("%d %c %f %lld %ld",sizeof(age), sizeof(star), sizeof(decimal), sizeof(num), sizeof(num1));
	
	return 0;
}
